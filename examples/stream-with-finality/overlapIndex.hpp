#ifndef OVERLAPINDEX_H
#define OVERLAPINDEX_H

#include <string>
#include <vector>

int overlapIndex(const std::vector<std::string>& prompt, const std::vector<std::string>& transcription);

#endif // OVERLAPINDEX_H