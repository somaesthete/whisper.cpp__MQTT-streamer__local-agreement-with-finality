#ifndef LOCALCONSENSUSBYN_H
#define LOCALCONSENSUSBYN_H

#include <vector>
#include <string>

std::vector<std::string> localConsensusByN(const std::vector<std::vector<std::string>>& buffer, int n);

#endif // LOCALCONSENSUSBYN_H